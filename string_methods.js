// methods
let name = " huraira faraz khan ";
let friend = "rabi";
let x = "                  mathmematics                           ";
alert(name.length);
alert(name.toUpperCase());
alert(name.toLowerCase());
alert(name.slice(2, 10));
alert(name.slice(0));
alert(name.replace("huraira", "uzair"));
alert(name.concat("is a boyfriend of ", friend));
alert(x.trim());

// quiz:
let y = "hamzakhan";
for (let i = 0; i < y.length; i++) {
  alert(y);
}
